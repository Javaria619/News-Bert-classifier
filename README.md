# News-Bert-classifier
Add transformer-based news classification project     Due to GitHub file size limits, trained model files are not included. Models can be reproduced by running the training script.
